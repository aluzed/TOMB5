# Linux32 — build neuf et parcours observable du 23 septembre 2026

Linux32/PSXPC_N est provisoire. Cette unité vérifie à nouveau le jalon intégré sur
le HEAD `cd95ce52` ; elle ne constitue ni une nouvelle reconstruction de la
caméra, ni une acceptation du jeu. PORT-001 à PORT-005, notamment PORT-004,
restent **In progress**.

## Exécution et attribution

- Build du jeu et de GLEW dans un dossier privé neuf par
  `python3 -B scripts/porting/linux32.py build`, avec l’archive GLEW 2.2.0
  contrôlée par son empreinte dans le driver. **build exit 0** ; le binaire
  construit est **ELF32 i386** ; ses bibliothèques i386 ont été vérifiées par
  le driver. L’identité binaire figure dans les ledgers privés.
- Un seul lancement du binaire fraîchement construit, sous Xvfb, avec les données
  locales CUE/BIN et une fenêtre identifiée. Entrées clavier **XTest**, captures
  directes de la fenêtre, sans écriture d’état du jeu ni saut de niveau :
  **29 s** titre ; `c` à 38 s pour Nouvelle partie ; **81 s** scène ; `Up` à
  83–96 s, **92 s** scène après entrée ; `Shift_L` puis `Right`, **104/107 s**
  vue noire ; **112 s** scène revenue après relâchement ; **117 s** pause.
- `WM_DELETE_WINDOW` à 120 s, puis **application exit 0** observé par le wrapper
  `timeout` avant sa borne de 170 s. Pas de GDB dans ce parcours : ni changement
  de salle ni état Lara ni disparition ultérieure de la fenêtre vérifiés ici.
  Un exit nul à lui seul n’est pas une validation gameplay.

L’audit des images fraîches repose sur la **comparaison de pixels** avec le
parcours antérieur dont les images avaient été inspectées et sur la chronologie
des entrées, **sans inspection visuelle directe** des pixels par le présent
agent. Les images 104/107 s sont presque entièrement noires ; la qualification
précise du contenu de chaque écran conserve cette limite. Les fichiers PNG,
binaire et données restent dans `build/reverse/runtime-20260923-1935/` ou un
autre chemin ignoré ; aucune capture ni donnée du jeu n’est versionnée.

## Tracker et readiness

- [x] Compilation complète ELF32 et GLEW réussie sur ce HEAD.
- [x] Une route réelle titre → chargement → scène → entrée clavier → regard
  défectueux → relâchement → pause → fermeture, avec captures et sortie bornée.
- [x] Le défaut observable concorde avec la limite connue : `LookCamera` est
  encore un **stub** de production ; aucun correctif source dans cette unité.
- [ ] Vue de regard fonctionnelle et équivalence cible non démontrées ; la preuve
  privée antérieure conserve des divergences de stockage GTE et des dépendances
  géométrie, animation et module spécial non fermées.
- [ ] Ni premier niveau complet, ni interactions, sauvegarde, audio matériel,
  manette et campagne validés ; aucune équivalence cible revendiquée.

## Handoff

Après ce contrôle de non-régression, le plus petit prochain travail technique
reste le contrat de composition `LookCamera` (voir
[camera-foundations.md](camera-foundations.md)) : borner les écarts GTE bruts
sans affaiblir les assertions, établir le producteur animé réel, la géométrie
et le dispatch des niveaux spéciaux sur les vrais types avant test RED de la TU
moteur et éventuelle intégration. Ne pas remplacer le stub sur la seule base
des captures. Le tableau historique de reconstruction RE reste terminal dans
son périmètre ; cette route portage ne le rouvre pas. Aucun ticket PORT Done.
