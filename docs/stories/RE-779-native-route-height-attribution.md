# RE-779 — Route native cohérente et attribution GetHeight

24 septembre 2026 (Europe/Paris). **Après RE-778**, une route native complète puis un témoin GetHeight réellement atteint relient désormais le caller, les données de pente et le retour consommé. C'est une attribution bornée, **pas une correction** : source de production inchangée, `code_change_readiness=blocked`.

## Tracker
- [x] RED documentaire avant story et section : trois absences attendues, exit 1 ; test public sans accès aux preuves privées.
- [x] Route native achevée récupérée après interruption du worker ; revue indépendante des archives lue, sans nouveau runtime par le publisher.
- [x] Attribution cible fraîche et capture native ultérieure distinguées ; dépendance du témoin concret reliée au corpus.
- [x] Dashboard append-only : overview et handoff actifs dans la nouvelle section ; anciennes transitions intactes.
- [ ] Équivalence universelle non établie ; gameplay complet non validé ; aucune intégration production justifiée.

## Route réelle : observations et échec conservé
Le rapport indépendant `build/reverse/autonomy-20260924-route-review-0935/REPORT.md` clôt sur archives une session de **1062 draws**, résultat route-complete, après interruption de la remise du worker. L'exécution native avait bien terminé sa route ; le HANDOFF producteur intermédiaire ne constituait pas son bilan final. Binaire existant ELF32 i386, pas de rebuild. Wrapper exit 0, application arrêtée par debugger-kill : **pas une sortie naturelle du jeu**.

Deux transitions **0→2→0** : demandes ItemNewRoom sous UpdateLaraRoom, application différée sous **KillMoveItems** avant le draw suivant. **762** chaînes UpdateLaraRoom observées, toutes alias lara_item, hauteur d'appel -381 : retour GetFloor transmis à GetHeight, puis retour GetHeight consommé dans item.floor. **822** appels GetCollisionInfo, **171** shifts non nuls au retour. Les 346 entrées ShiftItem n'ont pas de breakpoint de retour : application exacte de chaque translation non prouvée. Les deux cellules de transition sont à index zéro ; elles ne couvrent pas les branches complexes.

La revue a exécuté neuf méthodes d'archives : **8 PASS / 1 FAIL**, test original inchangé. Le FAIL attend zéro à l'**entrée du draw16**, où fade vaut encore 15 ; la capture après **présentation** vaut déjà zéro au draw16, et l'entrée du draw17 vaut zéro. C'est une confusion de points d'observation dans le contrat, pas un défaut persistant du fade démontré. Le probe route ne trace pas chaque entrée/retour de Fade. Deux scripts d'audit indépendants ont chacun exit 0, distincts des neuf méthodes. Libération naturelle au draw300, sept phases d'entrée réelle et 392 positions Lara distinctes ; la revue a inspecté des images montrant Lara et les décors, pas seulement un changement de caméra. Les messages Unimplemented! et les limites de collision restent présents.

## GetHeight : de la concordance corpus au pointeur natif
### Attribution cible, unité 0941
`build/reverse/autonomy-20260924-height-attribution-0941/HANDOFF.md` rapporte Ghidra frais, import et attribution du caller, GetHeight, helper et dispatch ; sortie headless exit 0. **6 appels** authentiques GetHeight jusqu'au retour, helper réel inclus, CPU/RAM neufs, 510 visites de code authentifiées, pas des instructions matériellement retirées. Corpus entrée3, room2, cellule8, index40 : **pente simple terminale**, inclinaison x nulle et z négative. Aux arguments **(46592,-376,35857)** : **retour5**, height_type=1, OnObject=0. Contrôles de coordonnées, index nul et pente neutralisée : sensibilité démontrée, mais mutations explicitement synthétiques. Le contrôle de fin de tuile conserve un pointeur direct, sans prouver sa sélection par le caller.

Les suffixes loader des entrées2 puis3 ont été exécutés ; entrée2 est une piste rejetée conservée. Le buffer complet pertinent concorde avec un décodage séparé. L'audit auteur vérifie six cas et RAM finales complètes avec stores inférés : **pas une revue indépendante**, pas une trace exhaustive des écritures transitoires. Cette première unité n'avait pas capturé floor_data natif : son résultat était une concordance corpus/cible/observation, pas encore l'identité des données pointées.

### Capture native, unité 0950
`build/reverse/autonomy-20260924-live-height-0950/HANDOFF.md` rapporte **un lancement frais de 402 draws**, même binaire, **pas de rebuild**. Le témoin exact est reproduit : **draw401, UpdateLaraRoom1834 → GetFloor1835 → GetHeight1836 → retour5 → item.floor5**, room2, cellule8, index40, arguments (46592,-376,35857). Le record item complet est archivé et le champ consommateur décodé.

Les données de descripteur/pente capturées à l'entrée native égalent celles de la **cible archivée** de 0941 ; cette unité native n'a pas fraîchement rejoué la cible. **10692 octets** depuis le pointeur natif égalent la région floor_data corpus entière. **borne corpus**, et non mesure indépendante de l'étendue de l'allocation native. Le buffer complet est capturé au **draw399**, sous GetCollisionInfo ; seuls les **quatre octets descripteur/pente** sont recapturés au **draw401**, témoin1836, au même pointeur. L'immuabilité intégrale du buffer entre ces deux observations n'est pas prouvée.

Le watchpoint d'affectation relie ce pointeur à **LoadLevel → S_LoadLevelFile(Name=1) → DoLevel(Name=1)**, en distinguant l'affectation du titre Name=0. Aucune nouvelle affectation du pointeur avant le témoin ; ce n'est pas un audit de toutes les écritures dans son buffer. Le cas est une **pente simple terminale sans pit**. L'ancien manque « floor_data natif non capturé » est donc fermé pour ce témoin, pas pour tous les chemins GetHeight.

Deux scripts d'assertions de cette unité ont exit 0 : contrat initial et audit du lien caller/données/cible archivée. Son **RED de setup**, résultat absent, est **pas comportemental** ; il ne démontre aucun défaut corrigé. L'image réelle a été inspectée par le worker natif : Lara visible dans une ruelle texturée, pas de vue noire. Capture X11 après swap, non preuve GPU/VRAM indépendante ni validation des collisions. Arrêt debugger après draw402, sans chaîne observée encore ouverte : pas une sortie naturelle du jeu.

## Portée, revues et validation publique
Publication metadata-only : **aucun asset public**, aucune donnée brute, adresse ou désassemblage. Publisher : lectures des rapports nommés et tests publics seulement, sans lancement ni replay privé. Pas de compilation fraîche des vraies TU, de trace exhaustive des lectures natives, de preuve matérielle PSX, de non-alias ou de tous les autres types floor_data. **équivalence universelle non établie**, **gameplay complet non validé**. Aucun correctif production justifié.

**Statut de revue courant : revue indépendante des deux unités GetHeight PASS borné, scellée à 10:01:11 Paris sous `build/reverse/autonomy-20260924-height-review-0955/` ; revue finale de publication PASS borné au snapshot qualifié, 71 tests publics PASS, clôturée à 10:07:07 Paris sous `build/reverse/autonomy-20260924-publication-review-1004/`.** Le reviewer des preuves privées a exécuté quatre scripts d'assertions, tous exit 0, sans nouvelle exécution cible/native : reconstruction indépendante des six RAM finales et 87 stores attendus, lien natif et conservation des archives. Sa qualification draw399/draw401 est incorporée ci-dessus. Les visites et appels cible restent ceux du producteur ; aucun replay privé parent n'est revendiqué.

Les **7 échecs historiques** de la suite reverse complète RE-778 (3347 PASS / 7 FAIL, identifiants conservés dans ses logs) restent divulgués ; cette suite globale n'est pas rejouée ici et n'est pas déclarée GREEN. La sélection ciblée exacte est récupérée de `build/reverse/autonomy-20260924-publication-0729/final-targeted.ledger.json` : RE-773 à RE-778 et générateur/dashboard, avec seul ajout du nouveau test RE-779. Commandes, environnement, début/fin, stdout/stderr, exit et hashes avant/après : `build/reverse/autonomy-20260924-publication-0955/`. Le test documentaire reste inchangé entre RED et GREEN ; les assertions textuelles contrôlent la publication, pas la vérité comportementale des archives.

Résultats publics : RED documentaire observé à 09:57:13 Paris, 3 échecs attendus en 0,08 s pytest, exit 1. GREEN à 09:59:06 : 3 PASS en 0,06 s pytest, exit 0. Sélection exacte du prédécesseur plus RE-779 : **71 PASS en 0,86 s pytest, exit 0**, du 09:59:06.472589 au 09:59:07.550530 Paris (1,077 s processus). Logs `red`, `green`, `suite` et ledgers homologues. Revalidation de cette même sélection après inscription des résultats : détails finaux dans le HANDOFF privé, sans modifier à nouveau le texte pour annoncer le résultat. Tests historiques inchangés ; aucune restauration de fichier nécessaire pour cette sélection ciblée.

## Handoff
**Après RE-779** : conserver cette chaîne route → caller → donnée native/corpus → GetHeight → item.floor comme jalon borné. Le prochain objectif proposé est un autre chemin non simple réellement atteint, avec préconditions et dépendances complètes, plutôt qu'une répétition de matrice ou un patch spéculatif. Ce handoff ne lance ni n'autorise seul cette recherche. L'état courant est dans cette story et la section RE-779 ; les anciennes recommandations restent historiques. Aucun staging, commit ou push par le publisher.
