# RE-780 — Correction bornée du filtre objet de GetHeight

25 septembre 2026 (Europe/Paris). **Après RE-779**, un trigger réellement atteint révèle une consultation indue des dépendances des actions non-objet. La **correction réelle du filtre objet**, quatre lignes intégrées par le parent dans `SPEC_PSXPC_N/GETSTUFF.C`, est désormais éprouvée sur la TU entière. Ce jalon ne certifie ni tous les callbacks ni tout GetHeight : **équivalence complète bloquée**, **gameplay complet non validé**.

## Tracker
- [x] RED documentaire réel avant cette story et son ajout HTML : trois échecs attendus, exit 1, 09:30:47 Paris ; contrats publics ensuite inchangés.
- [x] Preuve native, preuve cible, contrôles synthétiques et revues attribués séparément.
- [x] RED comportemental parent attesté avant correction, GREEN identique après intégration ; aucun nouveau patch source par le publisher.
- [x] Fixture publique revue, README corrigé, pytest actual-TU compilant la source réelle via le runner public sous build ignoré.
- [x] Dashboard append-only et historique préservé octet pour octet ; aucun compteur d'inventaire augmenté.
- [ ] Runtime du correctif intégré non établi dans ce snapshot ; fullbuild neuf bloqué.
- [ ] callbacks non nuls non réparés ; prochain objectif callback producer/consumer soumis à autorisation distincte.

## Preuve native — ancien binaire, pas le correctif intégré
La session `build/reverse/autonomy-20260925-nonsimple-native-0852/run01/` atteint naturellement le trigger au **draw300**, puis termine la route à **305 draws**. UpdateLaraRoom16 → GetFloor17 → GetHeight18 ; room0 cellule9, index5. Une action non-objet de type8 conduit pourtant cet **ancien binaire** à consulter item65 et object156, avec **callback nul** : aucun callback actif observé. Retour et item.floor valent zéro ; **0→0 non discriminant** pour l'exécution du store caller. Le record inchangé serait compatible avec son omission : pas de preuve sensible de cette écriture.

La revue `build/reverse/autonomy-20260925-trigger-native-review-0902/REPORT.md` rapporte **574 contrôles PASS** et quatre méthodes auteur PASS, sur archives préservées, sans nouvelle acquisition native. Le suffixe pré-instruction est partiel, pas une trace mémoire exhaustive. Les 10692 octets floor_data correspondent au corpus ; ce n'est pas une mesure indépendante de l'allocation. La revue a réellement inspecté la capture draw305 : Lara et ruelle visibles ; elle ne montre pas le moment exact du trigger et ne valide pas les collisions. L'ancien binaire fonctionne pour cette route, sans prouver le comportement du correctif. Arrêt debugger-kill volontaire, pas sortie naturelle du jeu. Le RED de setup résultat absent n'est pas un RED comportemental.

## Preuve cible — filtre discriminé, callback arrêté à sa frontière
L'unité `build/reverse/autonomy-20260925-trigger-target-0902/REPORT.md` exécute GetHeight cible authentifié sur le témoin natif : six retours et une frontière callback. Le témoin exact retourne zéro mais ignore l'action non-objet sans lire items/objects, contrairement au natif. Son suffixe loader et les données pertinentes sont reliés au corpus ; pas de nouveau runtime jeu natif.

La revue indépendante `build/reverse/autonomy-20260925-trigger-target-review-0910/REPORT.md` produit un probe distinct : **48 retours complets et 2 frontières** avant callback, soit cinquante cas, exit 0. Les sept cas auteur sont reproduits ; nouveaux contrôles synthétiques, CPU/RAM neufs par appel et instrumentation dynamique des stores. Seul le type objet consulte items/objects. Les types avec payload supplémentaire le consomment avant le filtre ; leur terminaison dépend du payload, non du mot précédent, et le payload n'est pas retraité en action objet. Le bit auxiliaire ne fait pas partie du type. Le record de sol exact et la géométrie utile concordent, mais pas tout le record room. Les accès source/cible ne sont pas intégralement identiques, notamment dans le cas objet inhibé. Unicorn reste un modèle logiciel, pas preuve matérielle.

## Preuve synthétique — TDD comportemental et compilation actual-TU
`build/reverse/autonomy-20260925-trigger-correction-0910/REPORT.md` rapporte le vrai RED avant modification privée : **153 cas, 42 échecs**, compilation/liaison réussies, exécutable exit 1 ; treize enfants signalent l'accès indu aux items protégés. Après quatre lignes de filtre, mêmes tests : 153 cas, zéro échec, exit 0. **24 observations** de régression callback restent identiques entre les deux sources : conservation du comportement actuel, pas réparation de ce contrat.

Le parent a ensuite exécuté ce même RED sur production avant intégration puis GREEN après intégration à 09:19. Les logs `build/reverse/autonomy-20260925-nonsimple-native-0852/parent-production-red.log` et `parent-production-green.log` conservent les compteurs 153/42 puis 153/0 ; la revue de code a vérifié leurs ledgers et exits 1/0. Ce RED comportemental est distinct du RED documentaire de publication.

Le runner `tests/reverse/fixtures/re780/run_test.py` compile la TU entière avec headers normaux, séparément du harnais ; ABI Linux/i386, liaison avec élimination des sections inutilisées sans suppression d'erreurs de symboles. Pas de copies de source dans un cwd temporaire, pas de corps GetHeight recopié. Le nouveau pytest `tests/reverse/test_re780_trigger_filter_actual_tu.py` invoque ce runner sur `SPEC_PSXPC_N/GETSTUFF.C` réel, sous `build/reverse/autonomy-20260925-publication-0928/pytest-actual-tu-*/`, avec logs, horaires, exit et hashes des entrées avant/après. Ce n'est pas un build du jeu entier. Les fixtures sont synthétiques, planes, sans pit ; les helpers liés ne sont pas nécessairement exercés. Pas de sanitizer ni de preuve universelle d'absence de comportement indéfini.

## Revue indépendante et limite callback
**Statut de revue courant : PASS borné du filtre et de la fixture**, `build/reverse/autonomy-20260925-trigger-code-review-0920/verdict.json` et `REPORT.md`. La compilation personnelle du reviewer à 09:21 produit 153 cas sans échec, exit 0 ; sortie égale au GREEN parent. La présente publication reste à relire indépendamment ; aucune revue finale anticipée. Le publisher relit les rapports et exécute les tests publics, sans jeu ni cible privés.

**callbacks non nuls non réparés** : la source passe l'adresse d'un local hauteur sans initialisation explicite et sans propagation de ce local au retour. La cible initialise son output avec la hauteur courante et le recharge après appel. Passer l'adresse seule ne constitue pas une lecture indéterminée ; cela dépend du callback réel. Le callback synthétique écrit seulement son output sans lire l'entrée et conserve délibérément le retour actuel. Deux arrêts à la frontière ne sont pas deux callbacks exécutés. L'équivalence complète bloquée demeure donc indépendante du filtre corrigé.

## Build frais, progression et validation
**fullbuild neuf bloqué** : `build/reverse/autonomy-20260925-trigger-runtime-fixed-0920/HANDOFF.md`. Configuration neuve réussie, build complet exit **124** après timeout ; reprise séquentielle terminée par **SIGKILL** (enfant -9, shell 247). Pression mémoire sévère observée, sans attribution causale exclusive. Aucun nouveau binaire complet ni runtime corrigé dans cette unité. L'alternative parallèle de 09:28 n'est pas supposée achevée et n'est pas intégrée à cette publication ; ce snapshot ne revendique aucune image ou capture fraîche du correctif.

Les **7 échecs historiques** reverse RE-778 et l'ancien **FAIL fade** restent conservés, non rejoués et non résolus ici : **pas de GREEN global**. Le FAIL fade distingue entrée du draw16 et présentation, non panne persistante démontrée. La sélection exacte prédécesseur de 68 tests, plus les trois RE-779 (71), est récupérée structurellement du ledger `build/reverse/autonomy-20260924-publication-0729/final-targeted.ledger.json`, puis augmentée seulement des quatre nouveaux tests RE-780. Les résultats et revalidations, commandes exactes, environnement, horaires, stdout/stderr, exits et hashes sont archivés dans `build/reverse/autonomy-20260925-publication-0928/` ; cette sélection ciblée n'est pas la suite reverse globale.

Résultats publics du publisher : GREEN ciblé RE-780 **4 PASS en 0,98 s pytest**, exit 0 ; sélection exacte **71 + 4 = 75 PASS en 1,76 s pytest**, exit 0, du 09:32:31 au 09:32:33 Paris. Ces quatre nouveaux tests comprennent trois contrats documentaires et un actual-TU ; ce dernier compile et exécute les **153 cas sans échec**, sans compter ces cas comme 153 tests pytest. Les résultats finaux après inscription sont conservés séparément dans `suite-final.log` et son ledger, sans redéclarer une validation globale.

Le dashboard conserve l'inventaire terminal historique et son générateur inchangés : overview et handoff actifs ajoutés dans la section RE-780, sans gonflement de table ni de compteur. Le titre ajouté utilise h3 pour rester compatible avec le contrat historique RE-779 sans modifier ce test ; le test RE-780 vérifie séparément la restauration byte-exacte du dashboard antérieur. Publication metadata-only : **aucun asset public**, aucune donnée brute, adresse historique, instruction extraite ou mot d'action authentique.

## Handoff
**Après RE-780** : filtre non-objet réellement corrigé et vérifié sur la TU complète, mais validation runtime corrigée encore ouverte dans ce snapshot. Prochain objectif **callback producer/consumer** : atteindre un callback réel avec préconditions et données complètes, distinguer initialisation de l'output, écriture par le producer, propagation au retour GetHeight puis consommation discriminante par le caller. Ne pas réparer ce contrat sans sa propre preuve et son propre RED comportemental. Aucun staging, commit, push ou cron par le publisher ; aucune autorisation implicite d'un nouveau chantier.
