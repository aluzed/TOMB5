# RE-366 runtime callee bridge candidate proof export

## Summary

Exported `11` source-symbolic context rows for candidate `a01f47cb95a4`.
No proof-domain is reopened by this export; the candidate hash still lacks direct source-backed proof.

## Proof gate

- candidate `a01f47cb95a4`: contexts `11`, direct repo symbols `0`, gate `blocked-unmapped-candidate-identity`, next `runtime-callee-bridge-candidate-callsite-map`

## Context families

- `audio-cd-service`: `3` rows
- `effects-spark`: `2` rows
- `floor-height-query`: `3` rows
- `item-room-runtime`: `3` rows

## Readiness decision

The export is metadata-ready, but source/domain readiness remains blocked until a source-backed callsite map can prove candidate-level behavior without raw binary evidence.
