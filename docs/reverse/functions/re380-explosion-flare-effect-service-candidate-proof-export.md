# RE-380 explosion/flare effect service candidate proof export

## Summary

Exported `18` source-symbolic context rows for candidate `87d9c8a62335`.
No proof-domain is reopened by this export; the candidate hash still lacks direct source-backed proof.

## Proof gate

- candidate `87d9c8a62335`: contexts `18`, direct repo symbols `0`, gate `blocked-unmapped-candidate-identity`, next `explosion-flare-effect-service-candidate-callsite-map`

## Context families

- `bubble-effect`: `1` rows
- `camera-context`: `2` rows
- `collision-effect-context`: `2` rows
- `entity-control-context`: `2` rows
- `flame-explosion-effect`: `3` rows
- `flare-service`: `2` rows
- `lara-runtime`: `2` rows
- `weapon-dynamics`: `4` rows

## Readiness decision

The export is metadata-ready, but source/domain readiness remains blocked until a source-backed callsite map can prove candidate-level behavior without raw binary evidence.
