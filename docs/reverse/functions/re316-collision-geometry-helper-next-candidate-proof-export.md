# RE-316 collision geometry helper next candidate proof export

## Summary

Exported `27` source-symbolic context rows for next candidate `d96359c1d9f3` after denied candidate `5e99f39fd8ef`.
No proof-domain is reopened by this export; the candidate hash still lacks direct source-backed proof.

## Proof gate

- candidate `d96359c1d9f3`: contexts `27`, direct repo symbols `0`, gate `blocked-unmapped-candidate-identity`, next `collision-geometry-helper-next-candidate-callsite-map`

## Readiness decision

The export is metadata-ready, but source/domain readiness remains blocked until a source-backed callsite map can prove candidate-level behavior without raw binary evidence.
