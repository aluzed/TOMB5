# RE-372 dynamic-lighting service candidate proof export

## Summary

Exported `23` source-symbolic context rows for candidate `f5d0099b5511`.
No proof-domain is reopened by this export; the candidate hash still lacks direct source-backed proof.

## Proof gate

- candidate `f5d0099b5511`: contexts `23`, direct repo symbols `0`, gate `blocked-unmapped-candidate-identity`, next `dynamic-lighting-service-candidate-callsite-map`

## Context families

- `alert-light`: `1` rows
- `creature-control`: `8` rows
- `dynamic-lighting-control`: `5` rows
- `flame-emitter`: `4` rows
- `lara-runtime`: `1` rows
- `torch-flare-lighting`: `3` rows
- `weapon-dynamics`: `1` rows

## Readiness decision

The export is metadata-ready, but source/domain readiness remains blocked until a source-backed callsite map can prove candidate-level behavior without raw binary evidence.
