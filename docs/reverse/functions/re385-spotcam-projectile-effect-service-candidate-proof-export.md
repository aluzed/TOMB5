# RE-385 spotcam/projectile effect service candidate proof export

## Summary

Exported `52` source-symbolic context rows for candidate `b6d128932004`.
No proof-domain is reopened by this export; the candidate hash still lacks direct source-backed proof.

## Proof gate

- candidate `b6d128932004`: contexts `52`, direct repo symbols `0`, gate `blocked-unmapped-candidate-identity`, next `spotcam-projectile-effect-service-candidate-callsite-map`

## Context families

- `ambient-effect-context`: `4` rows
- `camera-context`: `1` rows
- `entity-control-context`: `6` rows
- `flame-explosion-effect`: `8` rows
- `lara-runtime`: `4` rows
- `other`: `5` rows
- `projectile-bodypart-service`: `2` rows
- `rollingball-trap-service`: `1` rows
- `smoke-debris-effect`: `9` rows
- `spotcam-service`: `1` rows
- `trap-context`: `4` rows
- `weapon-dynamics`: `7` rows

## Readiness decision

The export is metadata-ready, but source/domain readiness remains blocked until a source-backed callsite map can prove candidate-level behavior without raw binary evidence.
