# RE-353 frontend display/menu service candidate proof export

## Summary

Exported `25` source-symbolic context rows for candidate `de919274685f`.
No proof-domain is reopened by this export; the candidate hash still lacks direct source-backed proof.

## Proof gate

- candidate `de919274685f`: contexts `25`, direct repo symbols `0`, gate `blocked-unmapped-candidate-identity`, next `frontend-display-menu-service-candidate-callsite-map`

## Context families

- `gpu-display`: `9` rows
- `inventory-menu`: `1` rows
- `level-load-service`: `6` rows
- `movie-playback`: `3` rows
- `platform-entry`: `3` rows
- `screen-setup`: `3` rows

## Readiness decision

The export is metadata-ready, but source/domain readiness remains blocked until a source-backed callsite map can prove candidate-level behavior without raw binary evidence.
