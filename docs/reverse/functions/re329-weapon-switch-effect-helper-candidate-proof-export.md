# RE-329 weapon-switch-effect helper candidate proof export

## Summary

Exported `17` source-symbolic context rows for candidate `1ddbda046e37`.
No proof-domain is reopened by this export; the candidate hash still lacks direct source-backed proof.

## Proof gate

- candidate `1ddbda046e37`: contexts `17`, direct repo symbols `0`, gate `blocked-unmapped-candidate-identity`, next `weapon-switch-effect-helper-candidate-callsite-map`

## Readiness decision

The export is metadata-ready, but source/domain readiness remains blocked until a source-backed callsite map can prove candidate-level behavior without raw binary evidence.
