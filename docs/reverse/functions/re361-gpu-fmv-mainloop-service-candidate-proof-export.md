# RE-361 gpu/fmv mainloop service candidate proof export

## Summary

Exported `14` source-symbolic context rows for candidate `1b3534d34062`.
No proof-domain is reopened by this export; the candidate hash still lacks direct source-backed proof.

## Proof gate

- candidate `1b3534d34062`: contexts `14`, direct repo symbols `0`, gate `blocked-unmapped-candidate-identity`, next `gpu-fmv-mainloop-service-candidate-callsite-map`

## Context families

- `gpu-display`: `5` rows
- `level-load-service`: `3` rows
- `movie-playback`: `3` rows
- `platform-entry`: `3` rows

## Readiness decision

The export is metadata-ready, but source/domain readiness remains blocked until a source-backed callsite map can prove candidate-level behavior without raw binary evidence.
